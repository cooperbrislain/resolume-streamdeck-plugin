{
  "name": "resolume-streamdeck-plugin",
  "version": "0.1.0",
  "description": "Stream Deck plugin for Resolume Arena/Avenue — live clip thumbnails and triggering",
  "main": "dist/plugin.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc && node dist/plugin.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0",
    "canvas": "^2.11.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "@vitest/coverage-v8": "^1.5.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
